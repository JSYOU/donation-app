import React from "react";

const LoadingSpinner: React.FC = () => {
  return (
    <div className="animate-spin rounded-full h-10 w-10 border-4 border-gray-300 border-t-red-600" />
  );
};

export default LoadingSpinner;
